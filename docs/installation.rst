============
Installation
============

At the command line::

    $ easy_install pre-commit-dotenv-checker

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pre-commit-dotenv-checker
    $ pip install pre-commit-dotenv-checker
